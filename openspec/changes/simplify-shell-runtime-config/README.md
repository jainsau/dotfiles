# simplify-shell-runtime-config

Simplify shell/runtime config by relying on Home Manager-provided XDG/EDITOR variables, removing brittle defensive fallbacks, and disabling the unused CLIProxy service.
