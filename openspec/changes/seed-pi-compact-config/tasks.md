## Tasks

- [x] Add a Home Manager activation hook that creates `~/.pi/agent/compact-config.json` only when missing.
- [x] Keep the seeded file mutable for Pi extension writes.
- [x] Seed `~/.pi/agent/compacti-config.json` as a compatibility alias for existing setup notes.
- [x] Run `nix flake check`.
