## 1. Configure Postman in nix-darwin

- [x] 1.1 Add `postman` to `homebrew.casks` list in `nix/darwin/default.nix`

## 2. Verify and Apply

- [x] 2.1 Run `nix flake check` to verify configuration syntax
- [x] 2.2 Verify that the `postman` cask is in the configuration list
