## 1. Insert Approval Gate in Workflow

- [x] 1.1 Update `.archon/workflows/development.yaml` to include `interactive: true` and the `approve-plan` approval gate

## 2. Verify and Apply

- [x] 2.1 Verify that the YAML is formatted correctly
- [x] 2.2 Run `archon workflow list` to ensure Archon compiles and validates the updated workflow successfully
