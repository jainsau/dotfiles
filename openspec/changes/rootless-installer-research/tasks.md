## 1. Research Devbox Installer

- [ ] 1.1 Analyze the devbox curl-to-bash script and Nix wrapper logic
- [ ] 1.2 Document the fallback mechanisms and edge case handling devbox uses

## 2. Research nix-portable

- [ ] 2.1 Test `nix-portable` locally without root access
- [ ] 2.2 Identify limitations of `nix-portable` compared to a standard Nix installation

## 3. Research proot for $HOME/nix

- [ ] 3.1 Attempt to map `/nix` to `$HOME/nix` using `proot`
- [ ] 3.2 Build and evaluate performance and compatibility implications of the proot overlay